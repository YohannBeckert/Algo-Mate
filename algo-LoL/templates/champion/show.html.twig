{% extends 'base.html.twig' %}

{% block title %}Champion{% endblock %}

{% block body %}
    <h1>Champion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ champion.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ champion.name }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ champion.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('champion_index') }}">back to list</a>

    <a href="{{ path('champion_edit', {'id': champion.id}) }}">edit</a>

    {{ include('champion/_delete_form.html.twig') }}
{% endblock %}
