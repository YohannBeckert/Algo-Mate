{% extends 'base.html.twig' %}

{% block title %}{% endblock %}

{% block body %}
<section class="top-content bg-size-show">
    <div class="background-content bg -{{ champion.name }}">
        <div class="content-item-div">
            <div class="title-champion">
                <h1>{{ champion.name }}</h1>
            </div>
            <a href="{{ path('champion_edit', {'id': champion.id}) }}">Editer</a>
            <a href="{{ path('champion_show', {'id':(champion.id + 1)}) }}">Next</a>
        </div>
    </div>
</section>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ champion.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ champion.name }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ champion.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('champion_list') }}">back to list</a>

    <a href="{{ path('champion_edit', {'id': champion.id}) }}">edit</a>

    {{ include('champion/_delete_form.html.twig') }}
{% endblock %}
